// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    
    repositories {
        google()
        jcenter()
           }
           
    dependencies {
        classpath 'com.android.tools.build:gradle:4.0.0'
    }
}
plugins {
 id("org.sonarqube") version "4.0.0.2929"
}

allprojects {
    repositories {
        google()
        jcenter()
        
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

sonarqube {
  properties {
    property("sonar.pullrequest.key", "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")
    property("sonar.pullrequest.base", "refs/heads/main")
    property("sonar.projectKey", "christosservos_ovaa")
    property("sonar.organization", "christosservos")
    property("sonar.host.url", "https://sonarcloud.io")
  }
}
